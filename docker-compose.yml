version: '3.8'

services:
  taro:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: taro-app
    restart: unless-stopped
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    networks:
      - taro-network

networks:
  taro-network:
    driver: bridge
